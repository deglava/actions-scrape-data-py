name: run main.py

on:
  schedule:
    - cron: "* * * * *"

jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v2 


      - name: printout  
        run: echo "tedrr"


